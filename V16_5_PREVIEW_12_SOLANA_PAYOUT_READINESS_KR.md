# SpaceNovaX V16.5 Preview 12 — Solana 자동 지급 준비

## 현재 상태

자동 지급 구조는 구현되어 있지만 기본값은 완전히 잠겨 있습니다. 현재 SPNX Point가 실제
SPNX 토큰으로 전송되지는 않습니다.

## 지급 조건

다음 조건을 모두 만족해야 자동 지급이 활성화됩니다.

1. 공식 Telegram Mini App에서 검증된 캡틴 계정
2. 전문 KYC 제공업체의 서명된 웹훅 결과가 `approved`
3. Phantom 또는 호환 지갑에서 서명한 지갑 소유권 증명
4. 공식 전환 기간 활성화
5. Solana RPC, SPNX SPL Token 민트, 재무 지갑 설정 완료
6. 서버 환경변수 `SOLANA_PAYOUTS_ENABLED=true`
7. 관리자 페이지에서 KYC, 전환 기간, 자동 지급 3단계 활성화

## 중복 지급 방지

- 전환 요청 시 SPNX Point를 `conversion_hold` 원장 항목으로 먼저 예약
- 요청당 고유 payout ID와 conversion ID 생성
- Solana 트랜잭션에 `SpaceNovaX:<payoutId>` 메모 기록
- 서명된 원시 트랜잭션과 트랜잭션 서명을 네트워크 전송 전에 저장
- 동일 트랜잭션만 재전송하며 만료 상태는 자동 재서명하지 않고 관리자 검토로 전환
- 완료된 지급은 다시 처리할 수 없음
- 아직 서명·전송되지 않은 지급 건만 관리자가 취소할 수 있으며, 보류된 SPNX Point는
  해시 체인 원장에 `conversion_release`로 복구
- 서명 또는 전송이 시작된 건은 즉시 취소하지 않고 반드시 온체인 상태부터 확인

## 지갑 보안

- 앱은 공개 지갑 주소와 서명 증명만 저장
- 시드 문구와 개인키는 요청하거나 저장하지 않음
- 재무 지갑 개인키는 Render 서버 비밀환경변수에만 저장
- 관리자 페이지에는 재무 지갑 개인키 입력란이 없음

## 반드시 남은 운영 작업

1. JSON 원장을 PostgreSQL 트랜잭션 원장으로 이전
2. 전문 KYC 업체 선정 및 실제 웹훅 규격 연결
3. Solana devnet 전체 지급 리허설
4. 다중서명 또는 HSM/KMS 기반 재무 지갑으로 전환
5. 전환 약관, 개인정보처리방침, 국가별 규제 검토
6. 메인넷 전 제한 금액으로 단계적 지급 테스트

## 필요한 환경변수

`.env.example`을 참고합니다. 비밀키와 API 키는 GitHub에 업로드하면 안 됩니다.
