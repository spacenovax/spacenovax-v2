# V15.1-PREVIEW-01 검토 안내

이 파일은 GitHub 배포 전 검토용입니다. 프리뷰 빌드는 실제 회원의 채굴 시작·잔액·보상을 변경하지 않습니다.

## NOVA AI 검토

1. 하단 `NOVA AI` 메뉴를 엽니다.
2. 선택형 질문 버튼이 없는지 확인합니다.
3. 일반 채팅처럼 질문을 직접 입력하고 전송합니다.
4. 두 번째 질문에서 첫 번째 질문의 내용을 이어서 물어봅니다.
5. 새로고침 후 대화가 남아 있는지 확인합니다.
6. 답변의 `Copy`와 `Read aloud` 버튼을 확인합니다.
7. 마이크 버튼으로 음성 입력을 확인합니다.
8. `New chat`으로 대화 초기화를 확인합니다.

프리뷰 빌드는 API 비용과 운영 데이터 변경 없이 화면을 검증하기 위해 NOVA 시험 응답을 제공합니다. 운영 빌드는 Render의 `OPENAI_API_KEY`와 `OPENAI_MODEL`을 사용합니다.

## 채굴 검토

- 초기 잔액: 0 SPNX
- 기본 보상: 30 SPNX / 24시간
- 기본 속도: 1.25 SPNX / 시간
- 프리뷰에서 채굴 버튼을 눌러도 실제 서버 원장은 변경되지 않습니다.

## 실행

```bash
npm install
npm run dev:preview
```

브라우저에서 `http://localhost:5173`을 엽니다.

## 승인 절차

프리뷰 검토 → 문제 수정 → RC 빌드 → 최종 승인 → GitHub main 업로드 → Render 배포 순서로 진행합니다.
